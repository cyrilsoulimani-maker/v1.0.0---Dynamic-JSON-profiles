<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ========================= -->
    <!-- Fenêtres -->
    <!-- ========================= -->

    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource Ds.Background}"/>
        <Setter Property="Foreground" Value="{StaticResource Ds.Text}"/>
    </Style>

    <!-- ========================= -->
    <!-- Texte -->
    <!-- ========================= -->

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Ds.Text}"/>
    </Style>

    <!-- ========================= -->
    <!-- Cards -->
    <!-- ========================= -->

    <Style x:Key="DsCard" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Ds.Panel}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Ds.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="Margin" Value="0,0,0,16"/>
    </Style>

    <!-- ======================================== -->
    <!-- Lists -->
    <!-- ======================================== -->

    <!-- DsListBox -->

    <Style TargetType="ListBox">
        <Setter Property="Background" Value="{StaticResource Ds.Panel}"/>
        <Setter Property="Foreground" Value="{StaticResource Ds.Text}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Ds.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <!-- DsListBoxItem -->

    <Style TargetType="ListBoxItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource Ds.Text}"/>
        <Setter Property="Padding" Value="8,4"/>
    </Style>

</ResourceDictionary>